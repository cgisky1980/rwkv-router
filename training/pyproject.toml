[project]
name = "router-head"
version = "0.1.0"
description = "Smart-router MLP head training (state-embedding classifier, ported from rwkv-router paper/scripts/03_classification.py)"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "numpy>=1.24",
]
